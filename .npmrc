# React Native Babel plugins (e.g. react-native-worklets) require() @babel/*
# packages at runtime but often don't declare them. Hoist so Metro can resolve
# them from the monorepo root under pnpm's strict layout.
public-hoist-pattern[]=@babel/*
