# Deploy-host configuration. Copy to deploy.env and fill in values.
# Sourced over SSH by scripts/deploy.sh and scripts/deploy-preview.sh.
# Never commit deploy.env — it contains secrets.

# Production
export PROD_DOMAIN=suro.app
export PORT=3000

# Shared Docker / Traefik
export PREVIEW_DOCKER_NETWORK=web

# Preview environments
# PREVIEW_HOST_DIR holds per-MR git working copies; keep it inside the project
# directory so everything stays together (gitignored as /previews/).
export PREVIEW_HOST_DIR=/srv/familia/previews
export PREVIEW_DOMAIN=preview.suro.app
export PREVIEW_ENV_FILE=/srv/familia/preview.env

