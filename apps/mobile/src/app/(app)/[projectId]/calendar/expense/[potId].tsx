/**
 * An expense pot opened from an event lives in the calendar stack (not the
 * expenses tab) so the back button returns to the event the user came from
 * rather than the expenses index. Renders the same screen as `expenses/[potId]`.
 */
export { default } from "../../expenses/[potId]";
