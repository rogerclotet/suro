/**
 * The editor for a note opened from an event, kept in the calendar stack for
 * the same reason as its viewer. Renders `notes/[noteId]/edit`.
 */
export { default } from "../../../home/notes/[noteId]/edit";
