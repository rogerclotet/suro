# Auto-loaded by fastlane. Safe defaults only — never put secrets here (use a
# gitignored fastlane/.env for the App Store Connect API key, etc.).
FASTLANE_OPT_OUT_USAGE=1
FASTLANE_SKIP_UPDATE_CHECK=1
